import Button from "../micro/Button";

export default function Header() {
  return (
    <header className="flex flex-col md:flex-row justify-end gap-4">
      <Button> G.H. COLLECTION </Button> 
    </header>
  )
}